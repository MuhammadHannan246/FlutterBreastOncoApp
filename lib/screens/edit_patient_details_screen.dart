import 'package:flutter/material.dart';

class EditPatientDetailsScreen extends StatelessWidget {
  const EditPatientDetailsScreen({super.key});
  static const routeName = '/edit-patient-details';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Text('Edit Patient Details'),
      ),

    );
  }
}